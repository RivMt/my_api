## What is this

This is API package for MySuite applications.

## API documentation

Every branch push builds the API reference with `dart doc` and uploads it as
a workflow artifact. Pushes to `master` also deploy the generated reference
to GitHub Pages.

To enable deployment, select **GitHub Actions** as the source under
**Settings > Pages > Build and deployment**.
