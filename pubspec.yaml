name: my_api
description: API package for MySuite applications
version: 0.0.1
homepage: https://github.com/RivMt

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http:
  shared_preferences:
  intl:
  decimal: ^3.0.2
  hooks_riverpod:
  easy_localization:
  fl_chart:
  oidc: ^0.9.0+1
  oidc_default_store: ^0.2.0+12
  flutter_svg: ^2.0.17

dev_dependencies:
  collection:
  test:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^2.0.0

